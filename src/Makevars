# link to PETSC, lapack, blas and a few more libraries
PKG_LIBS = -Wl,-rpath,$(PETSC_DIR)/lib -L$(PETSC_DIR)/lib  -L$(PETSC_DIR)/$(PETSC_ARCH)/lib -lpetsc -llapack -lblas -lpthread -lm -lgfortran -lm -lgfortran -lm -lquadmath -lm -lstdc++ -ldl -lgcc_s -ldl

# add include directories so that the c code can find the headers
PKG_CXXFLAGS = -I$(PETSC_DIR)/include -I$(PETSC_DIR)/$(PETSC_ARCH)/include -I$(PETSC_DIR)/include/petsc/mpiuni
